{% extends 'layout.nunjucks' %}

{% block content %}
  <h1>{{ title }}</h1>
  <p>Welcome to {{ title }}</p>

  <h1>Users</h1>
  <ul>
    {% for user in users %}
    <li> {{user.userid}} {{user.email}} {{user.username}} </li>
    {% endfor %}
  </ul>

{% endblock %}
